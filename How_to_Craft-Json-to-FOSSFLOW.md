# Как создать корректный JSON для FossFLOW

## Введение

FossFLOW поддерживает два основных формата диаграмм:
1. **Полный JSON формат** - детальный экспорт со всеми данными
2. **Компактный формат** - минималистичный формат для стабильного импорта

**Рекомендация:** Используйте **компактный формат** - он более надежен и проще в создании.

## 1. Компактный формат (рекомендуемый)

### Структура компактного формата

```json
{
  "t": "Название диаграммы",
  "i": [
    ["Имя элемента 1", "иконка", "описание"],
    ["Имя элемента 2", "иконка", "описание"]
  ],
  "v": [
    [
      [[индекс_элемента, x, y], [индекс_элемента, x, y]],
      [[индекс_от, индекс_до], [индекс_от, индекс_до]]
    ]
  ],
  "_": {
    "f": "compact",
    "v": "1.0"
  }
}
```

### Пример простой диаграммы

```json
{
  "t": "Веб-приложение",
  "i": [
    ["Пользователь", "user", "Посетитель сайта"],
    ["Веб-сервер", "server", "Nginx/Apache"],
    ["База данных", "storage", "PostgreSQL"]
  ],
  "v": [
    [
      [[0, 1, 4], [1, 3, 2], [2, 5, 2]],
      [[0, 1], [1, 2]]
    ]
  ],
  "_": {
    "f": "compact",
    "v": "1.0"
  }
}
```

## 2. Полный JSON формат

### Структура полного формата

```json
{
  "name": "Название диаграммы",
  "version": "1.0",
  "exportDate": "2025-12-25T12:00:00.000Z",
  "data": {
    "title": "Название диаграммы",
    "items": [
      {
        "id": "unique_id",
        "name": "Имя элемента",
        "icon": "иконка",
        "description": "Описание"
      }
    ],
    "views": [
      {
        "id": "view_id",
        "name": "Имя вида",
        "items": [
          {
            "id": "item_id",
            "tile": {"x": 1, "y": 2}
          }
        ],
        "connectors": [
          {
            "id": "conn_id",
            "anchors": [
              {"id": "a1", "ref": {"item": "item_id_1"}},
              {"id": "a2", "ref": {"item": "item_id_2"}}
            ]
          }
        ]
      }
    ],
    "colors": [
      {"id": "color1", "value": "#0066cc"}
    ],
    "icons": []
  }
}
```

## 3. Допустимые иконки

### ISOFLOW коллекция (37 иконок) - ОСНОВНАЯ
```
block, cache, cardterminal, cloud, cronjob, cube, desktop, diamond,
dns, document, firewall, function-module, image, laptop, loadbalancer,
lock, mail, mailmultiple, mobiledevice, office, package-module,
paymentcard, plane, printer, pyramid, queue, router, server,
speech, sphere, storage, switch-module, tower, truck, truck-2,
user, vm
```

### AWS коллекция (320 иконок)
Префикс: `aws-`
Примеры: `aws-ec2`, `aws-s3`, `aws-lambda`, `aws-rds`

### Azure коллекция (369 иконок)
Префикс: `azure-`
Примеры: `azure-vm`, `azure-storage`, `azure-functions`

### GCP коллекция
Префикс: `gcp-`
Примеры: `gcp-compute-engine`, `gcp-cloud-storage`

### Kubernetes коллекция
Префикс: `k8s-`
Примеры: `k8s-pod`, `k8s-service`, `k8s-deployment`

## 4. Инструменты для работы

### Валидация
```bash
# Полная валидация структуры
node validator.js your-file.json

# Проверка только иконок
node check-icons.js your-file.json
```

### Конвертация форматов
```bash
# Из полного в компактный
node convert-to-compact.js full.json compact.json

# Из компактного в полный
node test-compact-import.js compact.json full.json
```

## 5. Распространенные ошибки и исправления

### Ошибка: "Недопустимое поле"
**Причина:** Лишние поля в JSON
**Решение:** Удалите поля, которые не указаны в схеме

### Ошибка: "Иконка не найдена"
**Причина:** Использование невалидной иконки
**Решение:** Замените на иконку из списка выше

**Частые замены:**
- `web_app` → `server`
- `load_balancer` → `loadbalancer`
- `microservice` → `server`
- `redis` → `storage`
- `cdn` → `server`
- `shield` → `server`

### Ошибка: "Model item не найден в view"
**Причина:** View ссылается на несуществующий item
**Решение:** Проверьте соответствие ID в `data.items` и `data.views[*].items`

## 6. Лучшие практики

### Для надежности:
1. **Используйте только ISOFLOW иконки** - они всегда работают
2. **Проверяйте JSON перед импортом** валидаторами
3. **Используйте компактный формат** для импорта
4. **Держите координаты в разумных пределах** (x, y от -10 до 10)

### Для читаемости:
1. Используйте понятные названия элементов
2. Добавляйте описания для сложных компонентов
3. Группируйте связанные элементы рядом

## 7. Примеры готовых диаграмм

### Веб-инфраструктура (компактный формат)
```json
{
  "t": "Веб-сайт инфраструктура",
  "i": [
    ["Пользователь", "user", "Посетитель веб-сайта"],
    ["Интернет", "server", "Глобальная сеть"],
    ["DNS", "dns", "Доменные имена"],
    ["CDN", "server", "Content Delivery Network"],
    ["Firewall", "firewall", "Защита от угроз"],
    ["Load Balancer", "loadbalancer", "Распределение нагрузки"],
    ["Web Server", "server", "Nginx/Apache сервер"],
    ["App Server", "server", "Backend приложение"],
    ["Database", "storage", "PostgreSQL"],
    ["Cache", "storage", "Redis"]
  ],
  "v": [
    [
      [[0,1,6], [1,3,6], [2,5,6], [3,7,4], [4,7,8], [5,9,6], [6,11,4], [7,13,6], [8,15,6], [9,15,2]],
      [[0,1], [1,2], [1,3], [1,4], [2,5], [3,5], [4,5], [5,6], [5,7], [6,7], [7,8], [7,9]]
    ]
  ],
  "_": {"f": "compact", "v": "1.0"}
}
```

## 8. Тестирование

### Локальное тестирование
```bash
# Запуск FossFLOW
npm run dev

# Валидация файла
node validator.js your-diagram.json
node check-icons.js your-diagram.json

# Открыть тестовую страницу
python3 -m http.server 8080
# Перейти на http://localhost:8080/test-diagram-import.html
```

### Онлайн тестирование
1. Откройте FossFLOW в браузере
2. Нажмите "Import"
3. Выберите ваш JSON файл
4. Диаграмма должна загрузиться без ошибок

## 9. Полезные ссылки

- **FossFLOW:** http://localhost:3002 (после запуска)
- **Тестовая страница:** http://localhost:8080/test-diagram-import.html
- **Иконки:** Смотрите `icon-list-generation-guide.md` в папке FossFLOW
- **Схема:** `fossflow-schema.json` для полного формата

## Заключение

Для создания рабочих диаграмм FossFLOW:
1. Используйте компактный формат
2. Проверяйте иконки из ISOFLOW коллекции
3. Валидируйте JSON перед импортом
4. Тестируйте импорт в FossFLOW

Придерживаясь этих правил, ваши диаграммы будут открываться стабильно и без ошибок! 🚀
 
