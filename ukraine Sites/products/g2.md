## G2.com Listing & Pagination Mechanism — Full Breakdown

---

### 1. Rendering Model: Server-Side HTML + PJAX

G2 category pages are **server-side rendered (SSR)**. The initial page load at `/categories/crm` delivers a complete HTML document — no lazy-loaded API calls are needed for the first set of listings. There is **no separate JSON/GraphQL API** for listings.

When the user changes a filter or pagination, G2 uses **jQuery PJAX** (pushState + AJAX). PJAX fetches the new page via XHR, swaps only the `#ajax-container` div (not a full page reload), and updates the browser URL via `history.pushState`. This means:

- **First load** → normal GET, full HTML response
- **Subsequent filter/page changes** → XHR request to the same URL, server returns full HTML, but only `#ajax-container` content is swapped in the DOM

---

### 2. Request Structure

#### Base URL pattern
```
https://www.g2.com/categories/{slug}
```

#### PJAX XHR Headers sent on every filter/pagination change
```
X-PJAX: true
X-PJAX-Container: #ajax-container
X-Requested-With: XMLHttpRequest
X-CSRF-Token: <token>
```

The server detects the `X-PJAX` header and returns **only the inner HTML** for `#ajax-container` (not a full page). If you call the URL **without** that header (plain GET), you get a full HTML page — useful for scraping.

---

### 3. URL / Query String Parameters

All filters and pagination are passed as **GET query string parameters**:

| Parameter | Values | Description |
|---|---|---|
| `order` | `g2_score`, `popular`, `top_shelf` | Sort order |
| `page` | `1`, `2`, … `63` | Page number |
| `filters[star_rating][]` | `5`, `4`, `3` | Star rating filter (multi-select) |
| `filters[languages_supported][]` | `19`, `78`, `25`, `17`… (integer IDs) | Language filter |
| `filters[pricing_options][]` | `1` (Pricing Available), `2` (Deals Available) | Pricing filter |
| `filters[verified_features][]` | integer IDs | Feature filter |
| `filters[Standalone or All in One][]` | `36` (Standalone), `35` (All-in-One) | Deployment type |
| `utf8` | `✓` | Rails UTF-8 hidden field (can be omitted) |

#### Segment filter — separate URL path (not query param!)
Market segment is **not** a query param. It changes the URL path:

```
/categories/crm                  → All Segments (942 results)
/categories/crm/small-business   → Small Business (173 results)
/categories/crm/mid-market       → Mid Market (111 results)
/categories/crm/enterprise       → Enterprise (52 results)
```

---

### 4. Pagination Details

- **15 items per page** (confirmed: 942 total ÷ 63 pages ≈ 14.95)
- Pagination controls are `<ul class="pagination">` with standard numbered `<a>` links
- "Prev/Next" links are also present
- The paginator shows: `‹ Prev | 1 | 2 | 3 | 4 | 5 | … | 62 | 63 | Next ›`

---

### 5. How to Scrape Listings Programmatically

Since the content is SSR and the full HTML is available on plain GET (no browser needed for most cases), you can directly fetch pages:

```
# Page 1, default sort
GET https://www.g2.com/categories/crm?order=g2_score&page=1

# Page 2
GET https://www.g2.com/categories/crm?order=g2_score&page=2

# With filters: 5-star only, Small Business segment, page 3
GET https://www.g2.com/categories/crm/small-business?order=g2_score&page=3&filters[star_rating][]=5

# Multiple filters (4+5 star, pricing available)
GET https://www.g2.com/categories/crm?order=g2_score&page=1&filters[star_rating][]=5&filters[star_rating][]=4&filters[pricing_options][]=1
```

**To get only the listing fragment** (like PJAX does, faster/lighter response), add these headers:
```
X-PJAX: true
X-PJAX-Container: #ajax-container
X-Requested-With: XMLHttpRequest
```

This makes the server return only the swapped `#ajax-container` HTML, skipping nav, footer, etc. — a smaller payload. Note you'll also need a valid `X-CSRF-Token` for this to work server-side (grab it from any page's `<meta name="csrf-token">` tag first).

---

### 6. Summary Flow

```
User visits /categories/crm
    → Full SSR HTML delivered (15 listings in #ajax-container)
    → PJAX initializes on .pjax-form + .pagination links

User clicks filter checkbox / page number
    → Form serialized as GET params
    → XHR GET to same URL with X-PJAX headers
    → Server returns inner HTML only
    → jQuery PJAX swaps #ajax-container
    → URL updated via history.pushState (bookmarkable/crawlable URL)
```


## HTML/CSS структура G2 — полная разбивка

---

### Общая иерархия DOM (сверху вниз)

```
<div id="ajax-container" class="pjax-container x-pjax-initialized">
  <div class="no-border-radius">
    <div class="bg-white p-r">                          ← белый фон, relative
      <div id="product-list-sticky-container"
           class="category-products">
        <div id="product-list"
             class="cell large-12 pt-0 p-r">
          ...фильтры, заголовок, листинги...
        </div>
      </div>
    </div>
    <turbo-frame id="categories_grid"                   ← Hotwire Turbo (ленивый!)
                 src="/categories/crm/grids?from_overview=true"
                 loading="lazy">
      <div class="grid-container elv-animate-pulse">   ← skeleton loader
      <div class="grid-x grid-margin-x mt-3">          ← Foundation grid
    </turbo-frame>
    <div class="learn-content">                         ← SEO-текст внизу
  </div>
</div>
```

---

### Зона фильтров

**Основная форма фильтрации** — это несколько `<form>` с одинаковым action, работающих через PJAX:

```html
<form class="review-filter-form-get pjax-form flex jc-sb
             x-submit-on-change-initialized">
  <input type="hidden" name="utf8" value="✓">
  
  <!-- Фильтр-меню (Foundation Dropdown Menu) -->
  <ul class="dropdown menu flex fw-w gap-3 x-dropdown-menu-initialized">
    <li class="is-dropdown-submenu-parent opens-right">
      <div class="pill-menu">Segment ▾</div>        ← кнопка-пилюля
      <ul class="shadow-card menu menu--scrollable p-half mt-1
                 is-hidden-default submenu is-dropdown-submenu">
        <li class="menu is-submenu-item is-dropdown-submenu-item">
          <div class="my-4th">
            <label class="radio-label radio-label--standalone">
              <div class="d-f jc-sb c-midnight-100 text-sm">
                <div class="quantity-row">
                  <div class="quantity-row__title">
                    <span class="stars stars-10"></span>  ← звёзды-иконка
                  </div>
                  <div class="quantity-row__quantity">(142)</div>
                </div>
              </div>
            </label>
          </div>
        </li>
      </ul>
    </li>
    <!-- Rating, Language, Pricing, Features, Standalone — аналогично -->
  </ul>
  
  <!-- Sort By + счётчик -->
  <div class="ws-nw d-f fd-c ai-fe fw-semibold">...</div>
</form>
```

**Счётчик листингов:**
```html
<div class="l3 m-0 fw-regular">
  <span><strong>942</strong> Listings in CRM Available</span>
</div>
```

**Sort By dropdown:**
```html
<ul class="dropdown menu flex fw-w ai-c x-dropdown-menu-initialized">
  <li class="is-dropdown-submenu-parent">
    <div class="l4 fw-regular m-0 d-f ai-c cursor-pointer">
      <div class="mr-half">Sort By:</div>
      <strong>G2 Score</strong> ▾
    </div>
    <ul class="shadow-card menu p-half mt-1 is-hidden-default submenu">
      <li class="menu radios--dropdown is-submenu-item">
        <label class="radios__button">
          <input id="filters_sort_order_g2_score"
                 class="radios__input"
                 name="order" type="radio" value="g2_score" checked>
          G2 Score
        </label>
      </li>
      <!-- value="popular" → "Popularity" -->
      <!-- value="top_shelf" → "Satisfaction" -->
    </ul>
  </li>
</ul>
```

---

### Контейнер листингов — `#product-cards`

```html
<div id="product-cards"
     class="nested-ajax-loading
            segmented-shadow-card-container
            mb-1 mb-2-xlarge-up
            x-ordered-events-initialized">

  <script>...</script>   ← inline JS инициализации
  <script>...</script>

  <!-- Первая карточка (особый ID) -->
  <div id="category-list-first-product"
       class="x-ordered-inputs-initialized">
    <meta name="position" content="1">   ← порядковый номер
    <div class="segmented-shadow-card mx-0">
      ...
    </div>
  </div>

  <!-- Рекламная вставка -->
  <turbo-frame id="category_ad">...</turbo-frame>

  <!-- Карточки 2–15 -->
  <div class="x-ordered-inputs-initialized">
    <meta name="position" content="2">
    <div class="segmented-shadow-card mx-0">...</div>
  </div>
  ...
</div>
```

---

### Анатомия одной карточки продукта

```html
<div class="segmented-shadow-card mx-0">               ← внешняя обёртка с тенью

  <!-- Верхняя часть (основная информация) -->
  <div class="segmented-shadow-card__segment
              segmented-shadow-card__segment--multi-part">
    <div class="product-card x-software-component-card">

      <!-- Шапка карточки -->
      <div class="product-card__head">
        <div class="flex full-width">

          <!-- Логотип -->
          <div class="relative elv-h-fit">
            <a class="product-card__img js-log-click"
               data-event-options='{"product_id":506,"product_uuid":"...","product":"Salesforce Sales Cloud","vendor_id":469,"category_id":179,...}'>
              <img class="x-deferred-image-initialized"
                   data-deferred-image-src="https://images.g2crowd.com/...svg">
            </a>
            <!-- Pin/Wishlist иконка -->
            <div id="product-506-pin-icon"
                 class="simple-menu simple-menu--center">
              <a class="btn btn--secondary elv-rounded-full">📌</a>
            </div>
          </div>

          <!-- Название, компания, рейтинг -->
          <div class="product-card__title mb-1">
            <div class="product-card__info">
              <!-- Название продукта -->
              <div class="product-card__product-name">
                <a class="d-ib c-midnight-100 js-log-click"
                   href="/products/salesforce-salesforce-sales-cloud/reviews">
                  Salesforce Sales Cloud
                </a>
              </div>
              <!-- Компания -->
              <a class="a a--sm js-log-click"
                 data-test-id="tracked-seller-page-link">
                <span class="text-midnight">By Salesforce</span>
              </a>
              <!-- Звёзды + количество отзывов -->
              <a class="link flex fw-w text-small ai-c js-log-click">
                <div class="d-f ai-c fw-w">
                  <div class="stars stars-9"></div>   ← N*0.5 = рейтинг
                  <span class="pl-4th">(25,449)</span>
                  <span class="fw-semibold">4.4</span>
                  <span class="fw-semibold"> out of 5</span>
                </div>
              </a>
              <!-- Badges: "Optimized for quick response" -->
              <div class="flex ai-c x-optimized-for-response">...</div>
              <!-- Скидка/оффер -->
              <div class="product-card__coupon">
                <div class="coupon__tease">
                  <div class="coupon__ribbon">G2 Deals 25% Off...</div>
                </div>
              </div>
            </div>
          </div>

        </div>
      </div>

      <!-- Тело карточки (tabs: Overview / Pros and Cons / ...) -->
      <div class="full-height salesforce-sales-cloud-overview
                  x-stateful-container-initialized">
        <!-- вкладки -->
        <ul class="list list--spaced list--spaced--with-divider">...</ul>
      </div>
    </div>
  </div>

  <!-- Футер карточки (Compare + CTA кнопка) -->
  <div class="segmented-shadow-card__footer">
    <div class="flex jc-sb ai-c">

      <!-- Чекбокс Compare -->
      <div class="compare-form__button text-small x-monty-controls-initialized">
        <input id="product-compare-506"
               class="checkbox-button--no-border checkbox-button"
               type="checkbox">
        <label class="small fw-semibold">Compare</label>
      </div>

      <!-- CTA кнопка -->
      <form id="free-trial-submit" class="js-log-submit simple-bot-form-active">
        <button class="btn btn--width-auto btn--rorange x-loading-initialized">
          Try for free
        </button>
      </form>

    </div>
  </div>

</div>
```

---

### Пагинация

```html
<ul class="pagination">
  <li class="pagination__component pagination__component--disabled">
    <span class="pagination__only-elv">‹ Prev</span>   ← disabled на стр.1
  </li>
  <li class="pagination__component pagination__page-number
             pagination__page-number--current">
    1                                                    ← текущая (без ссылки)
  </li>
  <li class="pagination__component pagination__page-number show-for-large">
    <a class="pagination__page-number-link js-log-click pjax"
       href="/categories/crm?order=g2_score&page=2">2</a>
  </li>
  <!-- ... -->
  <li class="pagination__component pagination__component-gap">…</li>
  <li class="pagination__component pagination__page-number show-for-large">
    <a class="pagination__page-number-link js-log-click pjax"
       href="...&page=63">63</a>
  </li>
  <li class="pagination__component">
    <a class="pagination__named-link js-log-click pjax"
       href="...&page=2">Next ›</a>
  </li>
</ul>
```

---

### Ключевые классы для парсинга (CSS-селекторы)

| Что вытащить | CSS-селектор |
|---|---|
| Контейнер всех листингов | `#product-cards` |
| Одна карточка | `#product-cards > div.x-ordered-inputs-initialized` |
| Позиция в списке | `meta[name="position"]` → атрибут `content` |
| Название продукта | `.product-card__product-name a` |
| Href на страницу продукта | `.product-card__product-name a[href]` |
| Компания | `.product-card__info .a--sm .text-midnight` |
| Звёздный класс (рейтинг) | `.stars` → класс `stars-N` → рейтинг = N×0.5 |
| Числовой рейтинг | `.link.flex .fw-semibold:first-of-type` |
| Число отзывов | `.link.flex .pl-4th` |
| Логотип src | `img[data-deferred-image-src]` → атрибут `data-deferred-image-src` |
| product_id / product_uuid | `.product-card__img[data-event-options]` → JSON-parse атрибута |
| CTA кнопка текст | `.segmented-shadow-card__footer button, .segmented-shadow-card__footer a.btn` |
| Общий счётчик | `.l3.m-0.fw-regular strong` |
| Текущая страница | `.pagination__page-number--current` |
| Ссылка "следующая страница" | `a.pagination__named-link` |
| Числовые ссылки пагинации | `a.pagination__page-number-link` |
